{
  "name": "@anime-platform/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types/enums used by both apps/web and apps/api. Source-only workspace package (no build step) — consumers resolve index.ts directly.",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "types": "./index.ts",
      "default": "./index.ts"
    }
  }
}
